%h1
  Environments
- unless @notifications.empty?
  .alert
    = @notifications.join("<br />")
.row
  .span10
    %a.btn{:href => "/environments/create"}
      %i.icon-plus
      New
.row
- @environments.each do |env|
  .span2.well
    %a{:href => "/environments/#{env.id}"}= env.props["name"]
